/**
 * Deployment config — edit BACKEND_URL when frontend and backend are on different hosts.
 * Leave empty ('') when you open the app through the Flask/Render URL (same origin).
 *
 * Example (separate static hosting for Frontend):
 *   window.BACKEND_URL = 'https://online-test-platform.onrender.com';
 */
window.BACKEND_URL = '';
